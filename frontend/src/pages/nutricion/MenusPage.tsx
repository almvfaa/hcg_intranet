import React from 'react';
import FormularioMenu from '../../components/FormularioMenu';

const MenusPage = () => {
  return (
    <div className="menus-page">
      <h1>Gestión de Menús</h1>
      <FormularioMenu />
      {/* Aquí agregaremos la lista de menús existentes */}
    </div>
  );
};

export default MenusPage;
|